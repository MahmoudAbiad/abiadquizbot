# ملخص المراجعة والتعديلات

تاريخ المراجعة: 2026-07-19

## 1) سبب تعطّل "توليد الأسئلة من النص"
- الميزة كانت تعتمد **حصرياً** على مزوّد منفصل (Groq) بدل Gemini المستخدم لبقية الميزات.
- سجل Git يُظهر 8 محاولات متتالية فاشلة لتصحيح اسم الموديل ("groke model version fixed" ×8)،
  بينها أسماء غير موجودة أصلاً (خلط بين Grok من xAI وGroq).
- الخطأ الحقيقي من Groq كان يُبتلع بصمت ويظهر للمستخدم رسالة عامة فقط.

**الإصلاح (`helpers/gemini_helper.py`):**
- إضافة `_generate_text_quiz_with_gemini`: مسار بديل يولّد الأسئلة من النص مباشرة عبر Gemini
  (نفس المزوّد الموثوق المستخدم للملفات/الصور) دون رفع ملف، فقط بتمرير النص كمحتوى.
- `generate_quiz_smart` يجرّب Groq أولاً، وإن فشل (أو المفتاح غير مضبوط) ينتقل تلقائياً لـ Gemini.
- تحسين تسجيل الأخطاء ليوضح السبب الحقيقي في اللوج بدل الصمت.

## 2) سبب تعطّل "ألبوم الصور"
- آلية التنسيق عبر Redis (منسق واحد يجمع كل صور الألبوم) سليمة من حيث المبدأ، لكن:
  - لا يوجد معرّف تفرد (`file_unique_id`) لمنع معالجة نفس الصورة مرتين.
  - لا TTL على مفتاح قائمة الصور نفسه (فقط القفل له انتهاء صلاحية).

**الإصلاح (`handlers/files.py`):**
- إضافة `file_unique_id` لكل صورة + تصفية التكرار قبل التنزيل.
- إضافة `expire` على `album_list:*` لمنع تراكم بيانات يتيمة في Redis.

## 3) خلل مالي: لا استرجاع للنقاط عند فشل التوليد
- كانت النقاط تُخصم فور إدخال عدد الأسئلة، وإن فشل التوليد لاحقاً (لأي سبب) لا يوجد أي استرجاع.

**الإصلاح:**
- دالة جديدة `refund_user_points` في `helpers/supabase_helper.py`.
- `_run_quiz_flow` في `handlers/files.py` تسترجع النقاط تلقائياً عند الفشل وتُعلم المستخدم بذلك.

## 4) ميزة جديدة: التراجع/الإلغاء النظيف (المطلوبة في الطلب)
**في `handlers/files.py` و`my quiz bot/constants.py`:**
- زر "❌ إلغاء الطلب" يظهر تلقائياً مع:
  - رسالة استلام الملف/الصورة/الألبوم.
  - رسالة استلام النص المباشر.
  - رسالة قرار استخدام الكاش الجاهز (كخيار ثالث بجانب "استخدام الجاهز" و"توليد جديد").
- الضغط على الزر (أو إرسال أمر `/cancel`) في أي من هذه المراحل:
  - يحذف أي ملفات مرتبطة بالطلب من مجلد `downloads/` فوراً.
  - يمسح حالة المستخدم (FSM state) بالكامل.
  - يُظهر رسالة تأكيد واضحة، ويسمح بإرسال طلب جديد مباشرة (لا حاجة لإعادة تشغيل البوت).
- **حماية إضافية غير مطلوبة صراحة لكنها ضرورية لنفس الهدف:** إن أرسل المستخدم ملفاً/صورة
  جديدة بينما لديه طلب سابق معلّق لم يُبتّ فيه بعد (بانتظار العدد أو قرار الكاش)، يتم الآن
  إلغاء الطلب القديم تلقائياً وحذف ملفاته قبل قبول الجديد، بدل ترك الملف القديم "يتيماً" في
  `downloads/` للأبد.

## 5) ملاحظات هيكلية (لم يتم تعديلها الآن، تحتاج قرارك)
- `config.py` و`constants.py` موجودان داخل `my quiz bot/` بينما بقية الوحدات في الجذر. هذا يعمل
  فقط إذا كان `PYTHONPATH` على Railway مضبوطاً يدوياً ليشمل المجلدين معاً. يُفضّل نقلهما إلى
  جذر المشروع مستقبلاً لتفادي أي هشاشة عند أي تعديل على إعدادات النشر.
- تم حذف بقايا `__pycache__` القديمة (بينها ملف مترجم لـ `handlers/quiz.py` الذي لم يعد موجوداً
  في المصدر إطلاقاً منذ إعادة الهيكلة الأخيرة) — لا تأثير وظيفي، لكنه تنظيف مفيد.

## الملفات المعدّلة
- `handlers/files.py` (إعادة كتابة تتضمن ميزة الإلغاء + استرجاع النقاط + تحسين الألبوم)
- `helpers/gemini_helper.py` (بديل Gemini لتوليد الأسئلة من النص)
- `helpers/supabase_helper.py` (دالة `refund_user_points`)
- `my quiz bot/constants.py` (رسائل/أزرار الإلغاء الجديدة)

---

# ميزة جديدة: نمط الكويز المصوّر (LaTeX Image Quiz Mode)

تاريخ الإضافة: 2026-08-02

## الفكرة
عند إرسال الطالب ملفاً/صورة/صوراً أو نصاً يحتوي معادلات أو قوانين رياضية، يكتشف البوت
ذلك تلقائياً قبل التوليد، ويحوّل شكل الكويز بالكامل:
- كل سؤال يُرسم كصورة واحدة (سؤال + 4 خيارات) منسّقة بـ LaTeX لعرض المعادلات بشكل صحيح.
- الإجابة تكون عبر Poll منفصل يعرض فقط حروف الإجابة (أ/ب/ج/د أو A/B/C/D حسب لغة السؤال)،
  لأن محتوى السؤال والخيارات موجود بالكامل داخل الصورة أعلاه.

## آلية الكشف (`services/math_detector.py`)
- ملفات/صور: تُفحص أول صفحة (PDF) أو الصورة نفسها فقط، عبر نموذج Gemini خفيف وسريع
  (`gemini-3.5-flash-lite`)، بسؤال تصنيف قصير (yes/no).
- نصوص مباشرة (أو نص مُستخرج من أوفيس): تُفحص عينة موزّعة (بداية + وسط + نهاية، ~1500
  حرف) بنفس النموذج الخفيف بدل فحص النص كاملاً.
- فشل آمن: أي خطأ بالفحص = "لا يوجد محتوى رياضي" ولا يوقف تدفق التوليد العادي.

## توليد الأسئلة (`helpers/gemini_helper.py`, `constants.py`)
- موجّه توليد جديد `SYSTEM_PROMPT_GENERATE_MATH_QUESTIONS` يُستبدل تلقائياً بالموجّه
  العادي عند اكتشاف محتوى رياضي، يُلزم النموذج بصياغة أي معادلة بـ LaTeX ضمن `$...$`
  (مجموعة رموز مدعومة فقط: `\frac \sqrt \sum \int \lim` وحروف يونانية ومقارنات، دون
  `\begin{}`/`\text{}`/مصفوفات لأن محرك الرسم المستخدم لا يدعمها).
- لا قيود أحرف صارمة كالنمط النصي العادي (المحتوى يُرسم كصورة وليس Poll نصياً).

## رسم الصورة (`services/image_quiz_renderer.py` - وحدة جديدة)
- Matplotlib mathtext لرسم LaTeX دون الحاجة لتوزيعة LaTeX كاملة على السيرفر.
- دعم عربي RTL كامل (Reshape + Bidi) بنفس أسلوب `services/export_service.py`، مع تفافّ
  ذكي للأسطر يحافظ على كل مقطع رياضي `$...$` سليماً وغير مقسوم في المنتصف.
- فشل آمن لكل سطر: أي رمز LaTeX غير مدعوم يُعرض كنص عادي (تُجرَّد علامات $) بدل تعطّل
  توليد الصورة بأكملها.

## التكامل مع محرك الكويز (`services/quiz_engine.py`, `handlers/quiz_runner.py`)
- كل سؤال مُولَّد بنمط رياضي يُعلَّم بمفتاح `is_math: true` داخل `quiz_data` نفسه (بدل
  متغير حالة منفصل)، فينتقل تلقائياً مع الكويز عبر كل مسارات إعادة الاستخدام (الكاش،
  المفضلة، المشاركة) دون أي تعديل إضافي في `handlers/favorites.py` أو `handlers/sharing.py`.
- `send_quiz_poll` يتحقق من `is_math` ويحوّل التنفيذ لمسار الصورة + Poll الحروف تلقائياً.
- التلميح (`get_hint`) يُجرَّد من علامات `$` قبل عرضه في تنبيه Telegram العادي (لا يدعم LaTeX).

## تكامل الكاش مع Supabase (`helpers/supabase_helper.py`, `migration_math_image_quizzes.sql`)
- عمود جديد `is_math_quiz` على جدول `quizzes` (للفلترة/التقارير الإدارية فقط).
- باكت Storage عام جديد `quiz-images` لتخزين صور الأسئلة المرسومة.
- كل صورة تُرسم وتُرفع **مرة واحدة فقط**، ثم يُخزَّن رابطها العام داخل عنصر السؤال نفسه
  ضمن `quiz_data` (`image_url`)، فتُرسَل مباشرة بالمرات القادمة (لنفس الطالب أو غيره عبر
  ميزة الكاش الحالية) عبر `bot.send_photo(photo=<url>)` دون إعادة رسم أو رفع.
- تمييز الكويزات الرياضية بأيقونة 📐 في قائمة اختيار الكويزات الجاهزة (`keyboards.py`).

## الملفات الجديدة
- `services/math_detector.py`
- `services/image_quiz_renderer.py`
- `migration_math_image_quizzes.sql`

## الملفات المعدّلة
- `constants.py` (موجّهات الكشف والتوليد الرياضي)
- `helpers/gemini_helper.py` (باراميتر `is_math_mode`)
- `helpers/supabase_helper.py` (تخزين/كاش صور الأسئلة + عمود `is_math_quiz`)
- `services/quiz_service.py` (استدعاء الكشف السريع وتعليم الأسئلة)
- `services/quiz_engine.py` (مسار الصورة + Poll الحروف)
- `handlers/quiz_runner.py` (تمرير `quiz_id`، تنظيف التلميح)
- `keyboards.py` (أيقونة الكويز الرياضي)
- `requirements.txt` (إضافة `matplotlib`)

## ملاحظة معروفة (خارج نطاق هذا الطلب)
تصدير الكويز إلى Word/PDF (`services/export_service.py`) لم يُعدَّل: الكويزات الرياضية
المُصدَّرة ستُظهر نص LaTeX الخام (مثل `$x^2$`) بدل معادلة مرسومة، لأن ذلك يتطلب تكاملاً
إضافياً منفصلاً مع مكتبات الرسم داخل مسار DOCX/PDF لم يُطلب هنا.

---

# سبب ظهور طالبين فقط بلوحة الشرف + إعادة تصميم تدفق النشر والأزرار

تاريخ الإضافة: 2026-08-16

## 1) سبب المشكلة الأصلية (تحقّق من قاعدة البيانات الفعلية)
لا يوجد خلل بالكود. لوحة الشرف تعرض حتى 5 نتائج (`limit(5)` + مصفوفة ميداليات من 5 عناصر)
بدون أي سقف مصطنع. سبب ظهور طالبين اثنين فقط: النتيجة كانت تُحفظ `is_public = False`
افتراضياً عند إنهاء الكويز، ولا تصير عامة إلا بضغط الطالب زراً اختيارياً منفصلاً - وأغلب
الطلاب لم يضغطوه. تأكيد من `quiz_scores` الفعلي (مشروع `telegramquizbot`): كويز أخذه
10 طلاب، نشر نتيجته طالب واحد فقط؛ كويز آخر أخذه 8 طلاب، نشر اثنان فقط؛ عشرات الكويزات
الأخرى بصفر نشر رغم تعدد الآخذين لها.

## 2) إعادة تصميم تدفق النشر (بعد نقاش وتكرار على الفكرة - **حُدِّث مرتين**، هذا آخر تصميم)
**التصميم النهائي المطبَّق (2026-08-16، بعد تكرار ثانٍ على الفكرة):**
- شاشة نتيجة الكويز **لا تحتوي إطلاقاً** على أي سؤال أو زر متعلق بالنشر/الإخفاء - فقط
  زر واحد ثابت "🏆 عرض لوحة الشرف" (لكل المحاولات، أولى كانت أو متكررة). النتيجة تُحفظ
  خاصة (`is_public = False`) افتراضياً أول مرة، دون أي سؤال صريح للطالب في هذه اللحظة.
- **كل قرار النشر/الإخفاء صار حصراً تحت لوحة الشرف نفسها**: زر "🙈 إخفاء نتيجتي" أو
  "📢 انشر نتيجتي" حسب حالة الطالب الحالية.
- **الإضافة الأهم بهالتكرار:** الضغط على أي من الزرين لا يبدّل الكيبورد فقط - يعيد بناء
  **كامل رسالة لوحة الشرف** (النص + الكيبورد معاً عبر `edit_text`) فوراً، فتنعكس القائمة
  المحدَّثة (Top 5) مباشرة لحظة الضغط (مثلاً لو حدا نشر نتيجته ودخل ضمن أفضل 5، أو أخفاها
  ونزل عنها).
- ⚠️ **تصميم أول (`_end` / سؤال نعم-لا بشاشة النتيجة / `handle_decline_score`) اتبنى
  وطُبّق فعلياً بمرحلة سابقة، ثم أُلغي بالكامل واستُبدل بهذا التصميم بعد ملاحظة إضافية من
  المستخدم.** لا تشوف أي أثر له بالكود الحالي (`callback_data` صار بدون لاحقة مصدر إطلاقاً،
  البادئة `publish_score_`/`hide_score_` تُستخدم من مكان واحد بس - تحت لوحة الشرف).

**الإصلاح (`helpers/supabase_helper.py`):**
- `hide_score_from_leaderboard`: دالة لإخفاء النتيجة (عكس `publish_score_to_leaderboard`
  الموجودة أصلاً).
- `get_my_leaderboard_status`: ترجع حالة نشر نتيجة الطالب الحالية لكويز معيّن (أو `None`
  إن لم يأخذه أصلاً) لتحديد أي زر يظهر تحت لوحة الشرف.
- الحفظ الافتراضي عند أول محاولة صار `is_public=False` دائماً (لا يوجد أي مسار يجعلها
  `True` تلقائياً).

**الإصلاح (`keyboards.py`):**
- `get_quiz_result_keyboard` و `get_rating_keyboard`: صار الوسيط `quiz_id` فقط (بدون
  `show_publish_prompt` إطلاقاً بعد الإلغاء) - زر "عرض لوحة الشرف" الوحيد إذا `quiz_id` موجود.
- `get_leaderboard_keyboard`: زر تبديل الرؤية تحت رسالة لوحة الشرف (`is_public=None` يخفي
  الزر كلياً - يعني الطالب لم يأخذ هذا الكويز بعد).
- **إعادة تنظيم بصري لكل أزرار شاشة نتيجة الكويز** (كانت 9 صفوف منفصلة): دمج الأزرار
  المترابطة صفاً صفاً بجانب بعضها (لايك/ديسلايك، إعادة المحاولة/مشاركة، مفضلة/تحميل)
  ليصير المجموع 6 صفوف، مع تقصير نصوص الأزرار لتفادي التفاف النص عند التجاور.

**الإصلاح (`handlers/leaderboard.py` - إعادة كتابة كاملة):**
- `callback_data` بدون أي لاحقة مصدر (مصدر واحد فقط الآن: تحت لوحة الشرف).
- دالة موحّدة `_build_leaderboard_view(quiz_id, user_id)` تبني نص Top 5 + كيبورد تبديل
  الرؤية معاً - تُستدعى من أول عرض للوحة (`handle_show_leaderboard`) وبعد كل تبديل
  (`handle_publish_score`, `handle_hide_score`)، والتحديث دائماً عبر `edit_text` (نص+كيبورد
  معاً) مش `edit_reply_markup` لوحده - عشان القائمة تنعكس فوراً بالتغيير.
- `handle_decline_score` **حُذف بالكامل** (كان خاصاً بالتصميم الأول الملغى).

**الإصلاح (`handlers/quiz_runner.py`):**
- `_handle_quiz_completion`: أُزيل منطق `is_new_attempt`/`show_publish_prompt`/سطر السؤال
  الصريح بنص الرسالة بالكامل. `is_new_attempt` بقي فقط لتحديد عرض "نتيجتك السابقة" أو لأ
  (استخدام منطقي منفصل تماماً عن موضوع النشر).

## الملفات المعدّلة (آخر تكرار)
- `helpers/supabase_helper.py` (`hide_score_from_leaderboard`, `get_my_leaderboard_status`،
  تراجع الحفظ الافتراضي لـ `is_public=False`)
- `keyboards.py` (`get_quiz_result_keyboard`, `get_rating_keyboard`, `get_leaderboard_keyboard`
  - إزالة `show_publish_prompt` نهائياً، إعادة تنظيم الأزرار)
- `handlers/leaderboard.py` (إعادة كتابة كاملة: `_build_leaderboard_view` موحّدة + تحديث
  النص والكيبورد معاً بعد كل تبديل، حذف `handle_decline_score`)
- `handlers/quiz_runner.py` (إزالة سؤال النشر الصريح من `_handle_quiz_completion`)
