-- ============================================================
-- Migration: خصوصية أسماء المشاركين بالكويز الجماعي (show_names)
-- عمود جديد على group_quiz_sessions - يتحكم بس بشو البوت نفسه بيعرضه (رسائل
-- الترتيب النهائي/الدوري) - راجع handlers/group_quiz.py::finish_group_session.
-- ⚠️ لا علاقة له بزر "عرض من صوّت لمين" على الاستفتاء نفسه - هاد مفروض من
-- تيليجرام نفسها بمجرد is_anonymous=False (إجباري للتصحيح) وما في طريقة نطفّيه
-- من كودنا. القرار المعماري وتفاصيله موثّقة بمحادثة تنفيذ الخطوة 8.
-- آمن للتشغيل عدة مرات (IF NOT EXISTS).
-- ============================================================

ALTER TABLE public.group_quiz_sessions
    ADD COLUMN IF NOT EXISTS show_names BOOLEAN NOT NULL DEFAULT true;
