# تحديث: تسميات الأحداث الناقصة + تتبع التصدير/المفضلة + نظام الإحالات

امتداد على `ERROR_TRACKING_README.md` السابق — هذا التحديث لا علاقة له بالأخطاء، بل
بتوسيع تتبع **النشاط العادي** وسد فجوات كانت موجودة بالمشروع.

## 1) تسميات عربية لأحداث كانت مسجّلة بدون اسم مفهوم بلوحة الأدمن
11 نوع حدث كانت أصلاً تُسجَّل بجدول `usage_events` من قبل (مثل `audio_uploaded`,
`tutorial_opened`, `support_opened`...) لكنها كانت تظهر باسمها التقني الخام بشاشة
"الأحداث الأكثر تكراراً" لعدم وجود تسمية عربية لها بـ `EVENT_LABELS`
(`handlers/admin/analytics.py`). تم إضافة التسميات فقط — لا تغيير بالتتبع نفسه.

## 2) تتبع جديد: تصدير الكويز + فتح/حذف المفضلة
- `handlers/export.py`: عند نجاح توليد وإرسال ملف Word/PDF → `quiz_exported`
  (`format`, `style`, `title`).
- `handlers/favorites.py`: فتح كويز من المفضلة → `favorite_opened`، وحذفه →
  `favorite_deleted`.

## 3) تتبع جديد: الانضمام عبر رابط دعوة (إحالة)
المشروع كان أصلاً عنده نظام إحالة كامل (عمود `users.referred_by` + نقاط مكافأة
للمُحيل) لكن بدون أي حدث تحليلات له. أضفنا بـ `_add_new_user`
(`helpers/supabase_helper.py`): عند تسجيل مستخدم جديد بإحالة صحيحة → حدث
`joined_via_referral` مربوط بالمستخدم الجديد، يحتوي `referrer_id` و`referrer_name`.

⚠️ هذا الحدث **يبدأ من الآن فصاعداً فقط** (كباقي أحداث `usage_events`) — الإحالات
القديمة قبل هذا التحديث ليس لها حدث بالجدول، لكنها موجودة وتُحسب صحيحة بقسم "قائمة
الإحالات" التالي لأنه يعتمد على عمود `users.referred_by` الرسمي، وليس على الأحداث.

## 4) واجهة أدمن جديدة: 🎯 قائمة الإحالات
من `/admin` → 📈 تحليلات الاستخدام → **🎯 قائمة الإحالات**:
- قائمة بكل الطلاب الذين لديهم إحالة واحدة على الأقل، **مرتبة تنازلياً حسب عدد
  الإحالات** (10 بكل صفحة، مع أزرار تنقّل).
- كل طالب بالقائمة له زر **"👁 عرض إحالات"** يفتح قائمة منفردة (15 بكل صفحة) بأسماء
  من انضم عن طريقه تحديداً — لمنع ازدحام الواجهة الرئيسية بكل الأسماء دفعة واحدة، بالضبط
  كما طُلب.
- المصدر: `admin_get_referral_leaderboard()` بـ `helpers/supabase_helper.py` —
  يجمّع البيانات من عمود `users.referred_by` مباشرة (شامل تاريخياً)، لا حاجة لأي SQL
  migration جديد.

## الملفات المعدّلة بهذا التحديث
`handlers/admin/analytics.py`, `keyboards.py`, `helpers/supabase_helper.py`,
`handlers/export.py`, `handlers/favorites.py`
