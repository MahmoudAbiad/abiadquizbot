# سجل تطوير بوت الكويزات - abiadquizbot

**آخر تحديث:** هذا الملف يُحدَّث بشكل مستمر مع كل تعديل. أي AI (أو مطوّر) يكمل الشغل
لازم يبلش بقراءة هالملف كامل قبل أي تعديل جديد.

**قاعدة أساسية لأي AI تاني بيقرأ هالملف:** لا تفترض إنه الكود شغّال 100% لمجرد إنه
`py_compile` نجح. كل الشغل الموثّق تحت اتعمل وتحقق منه syntax-wise فقط (ما في بيئة
تشغيل فعلية متاحة للـ AI). **لازم** تسأل المستخدم شو جرّب فعلياً وشو نتيجة كل تجربة
قبل ما تبني فوق أي طبقة، خصوصاً الطبقات الحساسة مالياً (خصم نقاط، استرجاع).

---

## 🎯 السياق العام

المستخدم طلب تطوير بوت كويزات تيليجرام (aiogram v3 + Gemini AI + Supabase) بهدف:
1. تقليل عدد الخطوات/الرسائل اللي الطالب لازم يمرّ فيها قبل ما يبلش التوليد.
2. إضافة تخصيص لنوع الأسئلة (حسب المادة) ومستوى الصعوبة.
3. تحسين نظام الكاش (عرض تفاصيل + فلترة + سقف مستقل لكل تركيبة).
4. (لسا ما اتعمل) خيار "استخراج كامل الأسئلة من الملف" بتسعير لاحق آمن.

كل قرارات التصميم الأساسية اتحسمت بنقاش مطوّل قبل أي تنفيذ - ملخصها بقسم
"القرارات المعمارية المتفق عليها" تحت.

---

## ✅ الطبقات المنفذة (بالترتيب الزمني)

### الطبقة 1: التصنيف الموحّد + قاعدة البيانات
**الملفات:** `migration_quiz_options.sql` (جديد)، `services/subject_classifier.py` (جديد)،
`constants.py`، `helpers/gemini_helper.py`، `helpers/supabase_helper.py`، `services/quiz_service.py`

- استبدال استدعائي AI منفصلين (`services/math_detector.py` + `services/english_detector.py`
  - أصبحا **DEPRECATED**، غير مستوردين من أي مكان، أُبقيا كمرجع فقط) باستدعاء واحد موحّد:
  `services.subject_classifier.classify_subject()` يرجع `subject` (`math`/`english`/`other`)
  و`suggested_types` (اقتراحات AI ديناميكية 2-4 لو subject == "other").
- يستخدم Structured Output (`response_schema` + Pydantic) بدل نص حر yes/no.
- النتيجة تُخزَّن بـ FSM state (`subject_type`, `suggested_question_types`) وتُعاد قراءتها
  لاحقاً بـ `quiz_service.py` بدل إعادة الفحص.
- **قاعدة البيانات:** أعمدة جديدة على جدول `quizzes`: `subject_type`, `question_type`,
  `question_type_label`, `difficulty` + فهرس مركّب `idx_quizzes_combo`.
  **⚠️ الـ migration اتطبّقت فعلياً على مشروع Supabase الحقيقي** (`telegramquizbot`,
  project_id: `pqawoanhbgvdtbdhnbod`) عبر MCP، وتحقّقت من وجود الأعمدة. **آمنة 100%**
  (`ADD COLUMN IF NOT EXISTS` بقيم افتراضية، ما لمست بيانات موجودة).
- حقن `{difficulty_instruction}` و`{question_type_instruction}` بكل البرومبتات الأربعة
  الحالية بـ `constants.py` (عادي/رياضي/إنجليزي مترجم/إنجليزي عادي) - بدون تكرار برومبتات.
- `generate_quiz_smart()` بـ `gemini_helper.py` صار يقبل `difficulty`/`question_type`/
  `custom_question_type_text` ويحقنهم.

### الطبقة 2: شاشة اختيار نوع/صعوبة الأسئلة (راوتر جديد كامل)
**الملفات:** `handlers/quiz_options.py` (جديد بالكامل)، `config.py`، `keyboards.py`، `handlers/files.py`، `main.py`، `handlers/__init__.py`

- حالتان جديدتان بـ FSM: `waiting_for_quiz_options`, `waiting_for_custom_question_type`.
- **تصميم نهائي (بعد تعديل بناءً على ملاحظة ازدحام بصري):** مرحلتان متتاليتان بنفس
  الرسالة (edit_text)، مو شاشة واحدة فيها كل الأزرار مع بعض:
  1. **مرحلة النوع** (`get_quiz_type_keyboard`): أزرار حسب المادة (رياضيات: مسائل/قوانين/
     نظري، إنجليزي: قواعد/قراءة/اختبار عام، أخرى: اقتراحات AI ديناميكية) + "عام" + "تفضيل
     خاص" (نص حر). اختيار أي زر ينقل تلقائياً لمرحلة الصعوبة (بدون زر "متابعة" منفصل).
  2. **مرحلة الصعوبة** (`get_quiz_difficulty_keyboard`): سهل/متوسط/متقدم + رجوع. اختيار
     أي مستوى يكمل مباشرة لشاشة عدد الأسئلة.
- **زر "عام" العمومي مخفي لمادة الإنجليزي تحديداً** (لأن "اختبار عام" بالقائمة الجاهزة
  يؤدي نفس الغرض - كانوا يظهروا مع بعض كخيارين متطابقين مربكين، صار مصلّح).

**⚠️ خلل تم اكتشافه وإصلاحه (مهم لأي AI يعدّل هالملف لاحقاً):** لا تسجّل أبداً معالج
`F.data.startswith("qtype_")` قبل معالج `F.data == "qtype_custom"` (أو أي زر يبدأ بنفس
البادئة) - aiogram ينفّذ أول معالج مطابق بترتيب التسجيل ويتوقف، فالمعالج الأعم بيبلع
ضغطات المعالج الأخص قبل ما توصله. **الحل المعتمد:** معالج واحد فقط لكل بادئة (`qtype_`,
`qdiff_`) يفحص القيمة داخلياً بـ if/elif، بدل معالجات متعددة بفلاتر متداخلة. نفس المبدأ
لازم يُتبع لأي بادئة جديدة تُضاف مستقبلاً (بما فيها خيار "استخراج الكل" الجاي).
تم عمل فحص آلي شامل لكل `handlers/*.py` للتأكد من عدم وجود تصادمات مشابهة (نظيف حالياً).

### الطبقة 3: فلترة الكاش
**الملفات:** `keyboards.py`, `handlers/files.py`

- `get_multiple_quizzes_keyboard()` يعرض تفاصيل كل كويز مخزّن (نوع + صعوبة + عدد أسئلة).
- صفوف فلترة toggle (نوع/صعوبة) تظهر فقط لو فيه أكثر من قيمة فعلية موجودة بين الكويزات
  المخزّنة (تفادي ازدحام). الفلترة محلية بالكامل (Python list filtering على بيانات
  محمّلة أصلاً بالذاكرة من نداء واحد لقاعدة البيانات) - صفر استعلامات إضافية.
- دالة موحّدة `_render_cache_decision_screen()` تُستدعى من أول عرض وكل ضغطة فلتر.
- **سقف الكاش صار لكل تركيبة (نوع مادة × نوع أسئلة × صعوبة) على حدة** بدل سقف مشترك
  لكل الكويزات المخزنة للملف بغض النظر عن نوعها - دالة `combo_quiz_count()` بـ
  `services/quiz_service.py`.

### الطبقة 4: دمج شاشة عدد الأسئلة مع التأكيد (بعد ملاحظات مستخدم)
**الملفات:** `keyboards.py`, `handlers/files.py`, `config.py`

- **حُذفت حالة `waiting_for_generation_confirm` بالكامل** (والـ keyboard المرتبطة بيها
  `get_generation_confirm_keyboard` محذوفة كمان). التأكيد صار مدمج داخل شاشة
  `waiting_for_count` نفسها.
- الشاشة الحالية: أزرار عدد بدون سعر عليها (بعد ملاحظة مستخدم بتفضيل عدم الازدحام) +
  زر "✏️ عدد مخصص" + زر "🚀 ابدأ التوليد" بأسفل نفس الشاشة. **السعر يظهر حصراً بسطر
  "تكلفة العملية" ضمن نص الرسالة فوق الأزرار** (مصدر وحيد للسعر)، ويتحدّث تلقائياً مع
  كل تغيير بالعدد المختار لأن الرسالة كلها تُعاد بناؤها وتُعدَّل بالمكان (`edit_message_text`).
- دالة مركزية `_render_question_count_screen(bot, chat_id, message_id, state, target_for_send)`
  تُستخدم من كل نقاط الدخول (ضغطة زر، كتابة رقم يدوي) - نفس الرسالة تتحدّث بالمكان
  بغض النظر عن طريقة الإدخال، عبر `count_screen_chat_id`/`count_screen_message_id`
  المخزّنين بالـ FSM state.
- زر "🚀 ابدأ التوليد" (`qcount_start`) يدمج كل منطق التحقق من السقف + الرصيد + الخصم +
  استدعاء `execute_quiz_generation_workflow` بضغطة واحدة (كان سابقاً موزّع على معالجين
  منفصلين: `_process_question_count` + `handle_confirm_quiz_generation`، كلاهما محذوفان الآن).

**🩹 خلل موجود من قبل تعديلاتنا (اتصلّح بالمرة): استرجاع النقاط عند فشل التوليد
(`refund_user_on_failure`) كان بيقرأ نسخة قديمة من FSM state تم جلبها *قبل* إضافة
`debited_cost` إليها، فكانت قيمة الاسترجاع دائماً صفر.** تم تصحيحه بمزامنة الـ dict
المحلي يدوياً (`data["debited_cost"] = cost`) بعد `state.update_data(...)`.

**🩹 ثغرة تكرار الرسالة (اتصلّحت):** الضغط على نفس زر العدد المحدد مرتين متتاليتين كان
يسبب إرسال رسالة جديدة بدل تجاهل الضغطة، لأن تيليجرام يرفض `edit_message_text` بخطأ
"message is not modified" لو المحتوى الجديد مطابق حرفياً للقديم، وكان في `except: pass`
عام بيتعامل معها كفشل حقيقي ويطلع لمسار "أرسل رسالة بديلة". الحل: فحص نص الخطأ تحديداً
والتجاهل الآمن فقط لهالحالة تحديداً، بدون أي إرسال إضافي.

### الطبقة 5: إصلاح خلل حساب سقف الكاش (بعد بلاغ مستخدم "الكاش ما عم ينحسب صح")
**الملفات:** `handlers/files.py`

**الخلل:** فحص سقف التركيبة (`combo_quiz_count`) كان يصير بمكانين **قبل** ما التصنيف
(subject_type/question_type/difficulty) يصير أصلاً (`_render_cache_decision_screen` و
`_finalize_media_processing`) - بما إنه التصنيف مؤجَّل عمداً لغاية ما الطالب يضغط
"توليد جديد" (قرار معماري متفق عليه، راجع القسم أعلاه)، كان الفحص المبكر دايماً يتحقق
من قيم افتراضية ثابتة (`subject_type="other"`, `question_type="general"`,
`difficulty="medium"`) بغض النظر عن مادة الملف الحقيقية - نتيجة عشوائية وغير صحيحة
(ممكن يحجب زر "توليد جديد" بالغلط، أو يسمح فيه بالغلط، حسب محتوى الكاش المصادف).

**الإصلاح:** إلغاء الفحص المبكر كلياً بهالمكانين (`show_generate_btn = True` دايماً
بمرحلة عرض الكاش)، والاعتماد **حصراً** على الفحص الصحيح الموجود أصلاً بـ
`handle_count_start` (زر "ابدأ التوليد") - هذا المكان الوحيد اللي فيه `subject_type`/
`question_type`/`difficulty` معروفين فعلياً (بعد ما الطالب يعدّي على شاشة النوع/الصعوبة).
لو وصل فعلاً لسقف تركيبته الحقيقية، بيتوقف هناك برسالة `MSG_MAX_QUIZZES_REACHED` قبل
أي خصم - نفس الحماية المالية، بس بالمكان الصح فقط.

**⚠️ ملاحظة مهمة لأي AI يكمل:** `combo_quiz_count` لازم يُستخدم **فقط** بمكان بعد
التصنيف الفعلي (حالياً: داخل `handle_count_start` فقط). أي استخدام له بمكان قبل
`_ask_question_count` (يعني قبل ما `classify_subject` ينفّذ فعلياً) هو خطأ بنفس النمط -
تحقق دايماً من ترتيب التنفيذ الزمني الفعلي قبل قراءة `data.get("subject_type", ...)`
من الحالة، مو بس افتراض إنه القيمة موجودة لمجرد إنها متوقعة منطقياً.

### الطبقة 6: إصلاح خلل تسعير الكاش (بعد بلاغ مستخدم "الكاش عم يسكر عند عدد أسئلة معين")
**الملفات:** `keyboards.py`, `handlers/files.py`

**الخلل:** سعر الكويز المخزّن كان يُحسب **مرة واحدة فقط** من عدد أسئلة **أول كويز
بالقائمة** (`cached_quizzes[0]`) ويُخزَّن كقيمة وحيدة `cache_cost` بالحالة، وبعدين نفس
الرقم يُطبَّق على **أي** كويز يختاره الطالب من نفس القائمة - بغض النظر عن عدد أسئلته
الفعلي الخاص فيه. لو الكويز الأول بالقائمة كان أغلى (أسئلة أكتر) من كويز تاني بنفس
القائمة، طالب رصيده يكفي فعلياً للكويز الأرخص كان يتحجب بالغلط برسالة "رصيد غير كافٍ" -
هذا كان "إقفال الكاش" اللي لاحظه المستخدم، وبيصير فقط لما القائمة فيها كويزات بأعداد
أسئلة مختلفة (بعد إضافة تخصيص النوع/الصعوبة صار هذا الاحتمال شائعاً).

**الإصلاح:**
- `get_multiple_quizzes_keyboard()` بـ `keyboards.py` صار يقبل `items`/`is_album` (بدل
  `cost` واحد جاهز) ويحسب سعر **كل كويز على حدة** من `len(quiz['quiz_data'])` الخاص فيه،
  ويعرضه مباشرة على زره (`"... | 5 سؤال (2.50💎) | ..."`).
- `handle_multi_cache_selection()` بـ `handlers/files.py` صار يحسب `cost` من عدد أسئلة
  **الكويز المختار تحديداً** وقت الضغط، مو من قيمة `cache_cost` مخزّنة مسبقاً - وهذه
  القيمة (`cache_cost` بالحالة) أُزيلت كلياً من الكود (كانت مصدر الخلل).
- نص الرسالة برأس شاشة الكاش صار عام ("السعر موضّح على كل زر") بدل ذكر رقم واحد
  مضلِّل يوهم إنه نفس السعر لكل الكويزات.

**⚠️ ملاحظة مهمة لأي AI يكمل:** أي منطق مالي (سعر/تكلفة) لازم يُحسب دايماً من بيانات
**العنصر المحدد فعلياً وقت التنفيذ** (هون: عدد أسئلة الكويز المختار تحديداً)، وليس من
قيمة ممثِّلة/افتراضية محسوبة مسبقاً من عنصر آخر بنفس المجموعة (كان `[0]` هنا) - حتى لو
بدت متشابهة أو "من نفس الملف". هذا نفس نمط خلل الطبقة 5 أعلاه (قراءة قيمة من مكان/وقت
خاطئ بالتدفق) لكن بمنطق التسعير هالمرة بدل التصنيف.

### ملاحظة: بلاغ "الكاش سكر بعد 3 توليدات رغم اختلاف المواصفات" - نفس خلل الطبقة 5
المستخدم أبلغ عن هالسلوك **بعد** ما كانت خطة الإصلاح للطبقة 5 (أعلاه) لسا غير مطبّقة، أو
بنفس اللحظة تقريباً. تحققت بفحص شامل (`grep` لكل استخدامات `combo_quiz_count` و
`MSG_MAX_QUIZZES_REACHED` بكامل المشروع) إنه **الفحص الوحيد المتبقي حالياً** هو الصحيح
(داخل `handle_count_start`، يستخدم القيم الحقيقية المختارة فعلياً). هذا البلاغ غالباً كان
وصفاً لنفس أعراض خلل الطبقة 5 (الفحص المبكر الغلط بشاشة الكاش كان يقارن بتركيبة وهمية
ثابتة "other/general/medium" بدل التركيبة الحقيقية، فكان ممكن يوصل لسقفها بالغلط بعد عدد
توليدات معين حسب حجم الملف `items // 15`، بغض النظر عن مواصفات الطالب الفعلية) - وهو
مُصلَّح ضمن الطبقة 5. **لم يُكتشف أي سبب ثالث مستقل بعد الفحص الشامل.**
تنظيف إضافي بنفس الدفعة: أزلت متغيّر `max_allowed_quizzes` من `state` بـ
`_finalize_media_processing` لأنه صار كوداً ميتاً غير مستخدم بعد إزالة الفحص المبكر.

**إذا تكرر البلاغ بعد هالتحديث:** لازم معرفة (1) عدد صفحات/عناصر الملف بالضبط، (2)
تسلسل المواصفات الفعلي (نوع مادة/نوع أسئلة/صعوبة) لكل توليد بالترتيب، (3) هل السقف
انسد بشاشة الكاش (زر "توليد جديد" مخفي) أو عند الضغط "ابدأ التوليد" تحديداً (رسالة
`MSG_MAX_QUIZZES_REACHED`) - عشان نحدد الفرق بين الاثنين بدقة.

---

## 🧠 القرارات المعمارية المتفق عليها (السياق الكامل - لأي AI يكمل)

1. **الكاش:** يُعرض دائماً كل الكويزات المخزّنة للملف بغض النظر عن نوعها/صعوبتها،
   لكن سقف "التوليد المجاني الإضافي" (discount) مستقل لكل تركيبة (نوع مادة × نوع
   أسئلة × صعوبة) - `MAX_FILE_QUIZZES_LIMIT` لكل تركيبة على حدة.
2. **شاشة قرار الكاش تظهر قبل معرفة نوع/صعوبة الأسئلة المطلوبة** (التصنيف يصير فقط لو
   ما في كاش، أو لو الطالب اختار "توليد جديد" من شاشة الكاش) - هذا **متعمّد وليس خللاً**،
   اتفقنا عليه صراحة لتفادي سؤال الطالب عن التخصيص قبل ما يشوف إذا أصلاً في كويزات جاهزة.
3. **الصعوبة لا تؤثر على التسعير** (قرار صريح من المستخدم).
4. **فلترة الكاش محلية بالكامل** (لا استعلامات إضافية لقاعدة البيانات أو AI).
5. **الأمان المالي:** أي عملية توليد تخصم النقاط *قبل* الاستدعاء الفعلي لـ AI، وتسترجعها
   تلقائياً لو فشل التوليد (`refund_user_on_failure`). أي منطق جديد (خصوصاً "استخراج
   الكل" الجاي) لازم يحافظ على نفس المبدأ ولا يخصم بدون ضمان استرجاع عند الفشل.

---

## ⏳ المتبقي: خيار "استخراج كامل الأسئلة من الملف"

**لسا ما اتبلش فيه إطلاقاً.** هذا آخر جزء من الخطة الأصلية وأكبرهم تعقيداً لأنه يخالف
مبدأ التسعير المسبق المتبع بكل الشاشات الحالية.

### المشكلة الجوهرية (نوقشت مطوّلاً، القرار موثّق تحت)
النظام الحالي بالكامل مبني على **دفع مسبق**: نحسب التكلفة الدقيقة → نتأكد من الرصيد →
نخصم → نولّد → لو فشل نرجّع النقاط. لكن "استخراج الكل" يعني إننا **ما بنعرف العدد
الحقيقي إلا بعد التوليد الفعلي** - يعني التكلفة الحقيقية غير معروفة مسبقاً.

### الحل اللي اتفقنا عليه (ولسا ما اتنفذ):
1. **تقدير مسبق تقريبي (سقف أقصى نظري)** لعدد الأسئلة المتوقع، بناءً على عدد
   الصفحات/طول النص (فيه أصلاً `PAGES_PER_QUIZ_RATIO` بـ `constants.py` كمرجع مشابه).
2. **نتحقق فقط إن رصيد الطالب يغطي السقف الأقصى النظري** (مو التكلفة الفعلية اللي لسا
   مجهولة) قبل السماح بالبدء - لمنع أي طالب رصيده صفر من الدخول بمشوار توليد كامل
   بيكلّفنا فعلياً (استدعاء Gemini حقيقي) بدون أي مقابل.
3. بعد التوليد الفعلي، نحسب التكلفة الحقيقية (المفروض ≤ السقف لأنه اتأكدنا مسبقاً)
   ونخصمها فعلياً.
4. **لا تسعير ظاهر على الزر نفسه** (بعكس باقي الخيارات المسعّرة مسبقاً) - فقط
   **alert تأكيد** يوضح للطالب إن العدد النهائي غير مضمون ومرتبط بمحتوى الملف، وإن
   الخصم سيصير تلقائياً بعد التوليد حسب العدد الفعلي، وإن العملية ستفشل (بدون خصم) لو
   الرصيد ما يكفي حتى للسقف الأدنى المعقول.

### نقاط تصميم لازم تُحسم قبل التنفيذ الفعلي (لسا مفتوحة):
- إذا التوليد نجح لكن العدد الفعلي أكبر من المتوقع (نادر، بس ممكن) والرصيد الفعلي
  متبقي أقل من التكلفة الحقيقية بعد التوليد؟ (يفترض هذا السيناريو نادر جداً لأننا
  تحققنا من السقف الأقصى مسبقاً، لكن لازم قرار صريح: هل نخصم كل رصيده المتبقي فقط، أو
  نقص عدد الأسئلة المعروضة للطالب لتطابق رصيده الفعلي، أو نمنع الحالة أصلاً بجعل
  السقف الأقصى المتحقق منه مسبقاً أعلى بهامش أمان كافٍ؟).
- شكل الزر بالضبط بشاشة عدد الأسئلة (`get_question_count_keyboard`) - هل هو زر خامس
  إضافي بجانب أزرار 5/10/15/20، أم صف منفصل بعنوان مختلف؟
- نص الـ alert بالضبط (`answerCallbackQuery(show_alert=True)` غالباً، أو رسالة
  منفصلة بزر "متابعة/تراجع").
- **الأهم: لازم تسجيل هالبادئة الجديدة لأزرار الـ callback (اقترح `qextract_` أو
  مشابه) بحيث ما تتضارب مع أي بادئة موجودة (`qcount_`, `qtype_`, `qdiff_`,
  `cachefilter_`) - راجع قسم "خلل تم اكتشافه" أعلاه قبل تسجيل أي معالج جديد.**

---

## 🧪 حالة الاختبار الحالية

**لم يتم اختبار أي من الطبقات 1-4 على بيئة تشغيل حقيقية بشكل كامل بعد من طرفي (لا
يوجد وصول لتشغيل البوت فعلياً - فقط `py_compile` وفحوصات نصية آلية للتأكد من عدم وجود
أخطاء نحوية أو تضارب معالجات).**

المستخدم جرّب الطبقات 1-2 جزئياً ولاحظ واعدّل معي 4 مشاكل حقيقية (موثّقة أعلاه، كلها
مصلّحة الآن). **لسا ما تم تأكيد دورة كاملة ناجحة من طرف المستخدم** (رفع ملف → تصنيف →
نوع/صعوبة → عدد → توليد فعلي → عرض الكويز)، ولا اختبار مسار الكاش/الفلترة، ولا اختبار
حالات الإلغاء/الفشل.

**التوصية الحالية (متفق عليها مع المستخدم):** عدم البدء بخيار "استخراج الكل" لغاية ما
يتأكد المستخدم من 3 أشياء:
1. دورة كاملة ناجحة (رفع → تصنيف → نوع/صعوبة → عدد → توليد → عرض الكويز).
2. مسار الكاش (رفع نفس الملف تاني، عرض تفاصيل صحيحة، فلترة لو أكتر من كويز).
3. حالة إلغاء/فشل متعمّدة (زر إلغاء من كذا شاشة، التأكد من عدم خصم نقاط بالغلط).

---

## 📂 خريطة الملفات المتأثرة (مرجع سريع)

| الملف | الحالة | الدور |
|---|---|---|
| `migration_quiz_options.sql` | جديد | تعديل قاعدة البيانات (مُطبَّق فعلياً على Supabase الحقيقي) |
| `services/subject_classifier.py` | جديد | التصنيف الموحّد (يحل محل math_detector + english_detector) |
| `services/math_detector.py` | DEPRECATED | مُعلَّم فقط، غير مستورد من أي مكان |
| `services/english_detector.py` | DEPRECATED | مُعلَّم فقط، غير مستورد من أي مكان |
| `handlers/quiz_options.py` | جديد | راوتر شاشة نوع/صعوبة الأسئلة (مرحلتين) |
| `handlers/files.py` | معدَّل بكثافة | كل تدفق الاستقبال، الكاش، عدد الأسئلة المدمج، بدء التوليد |
| `handlers/__init__.py`, `main.py` | معدَّل | تسجيل `quiz_options_router` الجديد |
| `keyboards.py` | معدَّل بكثافة | `get_quiz_type_keyboard`, `get_quiz_difficulty_keyboard`, `get_multiple_quizzes_keyboard` (فلترة), `get_question_count_keyboard` (مدمجة). `get_generation_confirm_keyboard` **محذوفة** |
| `config.py` | معدَّل | حالات FSM جديدة، `waiting_for_generation_confirm` **محذوفة** |
| `constants.py` | معدَّل بكثافة | ثوابت الصعوبة/النوع، برومبت التصنيف، حقن البرومبتات |
| `helpers/gemini_helper.py` | معدَّل | حقن `{difficulty_instruction}`/`{question_type_instruction}` |
| `helpers/supabase_helper.py` | معدَّل | `get_file_quizzes`/`save_file_quiz_multiple` بأعمدة جديدة |
| `services/quiz_service.py` | معدَّل بكثافة | `combo_quiz_count`, `build_question_type_label`, `resolve_generation_question_type_text` |

---

## 🔧 معلومات تقنية للاستمرارية

- **Supabase project المستخدم فعلياً:** `telegramquizbot` (project_id: `pqawoanhbgvdtbdhnbod`) -
  فيه مشروع تاني بنفس الاسم تقريباً (`Test_telegramquizbot`) لكنه غير نشط، **لا تلمسه
  بالغلط**.
- **بادئات callback_data المحجوزة حالياً (لا تكرر أي وحدة فيها لأي زر جديد):**
  `qtype_`, `qdiff_`, `qback_to_type`, `qcount_pick_`, `qcount_custom`, `qcount_start`,
  `cachefilter_type_`, `cachefilter_diff_`, `cache_action_no`, `use_multi_`,
  `cancel_upload_request`.
- **قبل تسجيل أي معالج callback جديد:** شغّل هالفحص للتأكد من عدم وجود تصادم بادئات
  (استبدل المسار حسب الحاجة):
  ```python
  import re, os
  startswith_filters, exact_filters = [], []
  for root, dirs, files in os.walk('handlers'):
      for fn in files:
          if not fn.endswith('.py'): continue
          path = os.path.join(root, fn)
          src = open(path, encoding='utf-8').read()
          for m in re.finditer(r'@router\.callback_query\([^)]*F\.data\.startswith\(\"([^\"]+)\"\)', src):
              startswith_filters.append((path, m.group(1)))
          for m in re.finditer(r'@router\.callback_query\([^)]*F\.data == \"([^\"]+)\"', src):
              exact_filters.append((path, m.group(1)))
  for f_ex, val in exact_filters:
      for f_sw, prefix in startswith_filters:
          if val.startswith(prefix) and val != prefix:
              print(f'COLLISION: "{val}" ({f_ex}) shadowed by startswith("{prefix}") ({f_sw})')
  ```
- **بعد أي تعديل:** دايماً شغّل `python3 -m py_compile <كل الملفات المعدّلة>` وتحقق من
  كل الاستيرادات من `constants.py` فعلياً معرّفة (نفس أسلوب الفحص المستخدم بكل الطبقات
  أعلاه) قبل التسليم.
