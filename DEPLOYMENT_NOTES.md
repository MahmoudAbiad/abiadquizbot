# ملاحظات النشر متعدد المنصات

**البوت يتنقّل فعلياً بين 4 منصات: Render, Heroku, Railway, Azure.** هذا الملف مرجع
موحّد لملاحظات النشر التي كانت مبعثرة كتعليقات عابرة داخل الكود (`main.py`,
`webhook_server.py`, `handlers/audio.py`, `helpers/gemini_helper.py`, `config.py`,
`mem_utils.py`, `constants.py`...). **لا يُلغي** `AZURE_DEPLOYMENT.md` (دليل نشر Azure
الكامل والمستقل) - هذا الملف تكميلي فقط.

---

## ⚠️ يحتاج تعبئة من المستخدم (لا يمكن استنتاجه من الكود وحده)

- [ ] **أي منصة نشِطة فعلياً الآن؟**
- [ ] **متى/لماذا تم الانتقال بين المنصات سابقاً؟** (مفيد لأي AI/مطوّر يحقق ببلاغ خلل
      شبيه بمشكلة منصة قديمة)
- [ ] **هل كل الإصلاحات الخاصة بمنصة واحدة (تحت) تم التحقق من صحتها/ضرورتها على
      المنصات التالتة؟**

---

## 🧩 ما يجعل الكود نفسه متنقّلاً بين المنصات (بدون تعديل) - محايد فعلياً

`main.py` و`webhook_server.py` يقرآن كل شيء من متغيرات بيئة (`PORT`, `WEBHOOK_URL`,
`WEBHOOK_PORT`) ويقرران وضع Webhook أو Polling تلقائياً - **لا حاجة لتعديل كود عند
تغيير المنصة نفسها**، فقط متغيرات البيئة + ملف النشر المناسب للمنصة (تحت).

## 📄 أي ملف نشر يخص أي منصة

| المنصة | ملف النشر المتوقّع | موجود بالمشروع؟ | ملاحظة |
|---|---|---|---|
| Heroku | `Procfile` + `app.json` (buildpack `heroku/python`) | ✅ | جاهز مباشرة |
| Railway | عادة يقرأ `Procfile` أو `Dockerfile` (وليس `app.json`) | ✅ (عبر أحدهما) | لم يُتحقق أيهما يُستخدَم فعلياً على Railway تحديداً |
| Render | عادة يقرأ `Dockerfile` أو `render.yaml` | ⚠️ `Dockerfile` موجود، لا `render.yaml` | تحقق قبل أي نشر جديد على Render |
| Azure | حسب `AZURE_DEPLOYMENT.md` (App Service / Container) | ✅ موثّق بالكامل | راجع الملف مباشرة |

## 🩹 إصلاحات/ضبط مرتبطة بمنصة محددة (تحتاج إعادة تحقق عند تغيير المنصة)

هذه ملاحظات وُجدت متفرقة بتعليقات الكود - **لم يتم التحقق من صحتها/ضرورتها على
المنصات الثلاث التانية غير المذكورة بكل بند**:

1. **`config.py` (Redis idle connection fix):** الإصلاح (`health_check_interval`,
   `socket_keepalive`, `retry_on_error`) صُمِّم لمشكلة رُصدت مع **Upstash عبر Heroku**
   تحديداً (يقفل الاتصالات الخاملة بصمت). إذا انتقل مزوّد Redis نفسه (ليس فقط منصة
   الاستضافة)، تحقق أن نفس السلوك موجود.
2. **`mem_utils.py` + `Dockerfile` (`MALLOC_ARENA_MAX=2`, `MALLOC_TRIM_THRESHOLD_`):**
   مضبوط لحاوية بحد ذاكرة صغير (**Heroku/Docker**). تحقق من حد الذاكرة الفعلي على أي
   منصة أخرى - قد يكون التضييق زائداً عن الحاجة (أو غير كافٍ) بحد مختلف.
3. **`helpers/gemini_helper.py`:** تعليق يذكر "Memory Exceeded على **Render**" تحديداً
   كسبب لتصميم الـ Singleton client - السلوك نفسه (تسريب ذاكرة عند إنشاء عميل جديد بكل
   نداء) عام وليس خاصاً بـ Render، لكن جرى رصده هناك أول مرة.
4. **`handlers/audio.py` (ffmpeg/ffprobe):** الإصلاح مصمَّم أصلاً لغياب `ffprobe` على
   نظام **Heroku buildpack** - لكنه يعمل بأي بيئة لأنه لا يعتمد على apt/النظام إطلاقاً
   (ثنائي ساكن عبر `imageio-ffmpeg`)، فهذا البند غالباً آمن على الأربع منصات بلا تعديل.
5. **`constants.py` (دومين رفع الويب):** التعليق يذكر أن الدومين "نفس دومين الويبهوك
   المستخدم أصلاً على **Heroku**" - **تحقق يدوياً أن `WEBAPP_PUBLIC_BASE_URL` مضبوط
   فعلياً لأي منصة نشِطة حالياً**، هذا أكثر بند بالقائمة عرضة لكسر صامت عند تبديل
   المنصة (رابط قديم لا يعمل بدل خطأ واضح).

---

*هذا الملف وليد فحص آلي للكود + توضيح من المستخدم أن النشر يتنقل بين 4 منصات -
البنود المُعلَّمة "يحتاج تعبئة" لازم تُحدَّث يدوياً، والبند 5 تحديداً يستحق تحققاً
فورياً قبل أي نشر جديد.*
