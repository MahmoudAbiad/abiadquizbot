# تتبع أخطاء الطلاب بلوحة الأدمن — دليل التركيب

نظام التحليلات الحالي (`ANALYTICS_README.md`) كان يسجل نشاط الطالب (بدء بوت، رفع ملف،
توليد كويز...) لكنه **لا يسجل الأخطاء** التي يواجهها الطالب أثناء الاستخدام. هذا التحديث
يضيف تسجيلاً تلقائياً وشاملاً لكل خطأ يواجهه أي طالب، دون الحاجة لتعديل مئات نقاط
`try/except` الموجودة سلفاً بكل الملفات.

## 1) لا يوجد أي SQL Migration جديد مطلوب
النظام يستخدم نفس جدول `usage_events` الموجود أصلاً (نفس البنية: `user_id`, `event_type`,
`metadata` JSONB) — فقط بنوع حدث جديد `error_occurred`. لا حاجة لأي تعديل بقاعدة البيانات.

## 2) كيف يعمل (الفكرة الأساسية)
أضفنا ملفاً جديداً `error_context.py` يحمل "سياق الطلب الحالي" (معرّف المستخدم صاحب
الرسالة/الضغطة الحالية) عبر `contextvars`. الميدل وير الجديد `ErrorTrackingMiddleware`
(في `middlewares.py`) يضبط هذا السياق تلقائياً في بداية كل تحديث تيليجرام (رسالة، ضغطة
زر، إجابة استفتاء كويز...) ويُصفّره بالنهاية.

النتيجة: أي استدعاء لدالة `log_error()` أو `log_critical()` الموجودة أصلاً بمئات الأماكن
بالمشروع (`log_error(logger, f"Error in X: {e}", exception=e)`) يقرأ هذا السياق تلقائياً
ويسجّل الخطأ كحدث `error_occurred` مرتبط بنفس الطالب — **بدون أي تعديل يدوي على تلك
النقاط**. هذا يغطي فعلياً كل أخطاء التطبيق الحالية والمستقبلية طالما استخدمت `log_error`.

بالإضافة لذلك، نفس الميدل وير يعمل كشبكة أمان أخيرة: أي استثناء "غير متوقع بالكامل" يهرب
من كل الـ handlers (كود لم يكن يحتوي على `try/except` أصلاً) — وكان سابقاً يختفي بصمت
ويترك الطالب بدون أي رد — الآن يُسجَّل أيضاً (`unhandled: true` بالـ metadata)، ويحاول
النظام إرسال رسالة اعتذار عامة للطالب بدل تركه بدون رد إطلاقاً.

## 3) الملفات الجديدة/المعدّلة

| الملف | التعديل |
|---|---|
| `error_context.py` (جديد) | سياق الطلب الحالي (contextvars) |
| `middlewares.py` | إضافة `ErrorTrackingMiddleware` (تتبّع + شبكة أمان أخيرة) |
| `main.py` | تسجيل `ErrorTrackingMiddleware` كـ `dp.update.outer_middleware` |
| `logger.py` | `log_error()`/`log_critical()` تربطان تلقائياً بسياق الطالب الحالي |
| `helpers/supabase_helper.py` | دالة `log_error_event()` جديدة + `admin_get_recent_errors()` + إضافة `error_count`/`users_with_errors` لملخص `admin_get_usage_overview()` + إصلاح نقطة كانت ستسبب حلقة استدعاء ذاتية عند فشل التسجيل الاحتياطي |
| `handlers/admin/analytics.py` | زر/قسم جديد "🐞 آخر الأخطاء" + عداد الأخطاء بشاشة التحليلات العامة + تسمية حدث `error_occurred` |
| `keyboards.py` | زر "🐞 آخر الأخطاء" بلوحة التحليلات |

## 4) ما الذي تراه بلوحة الأدمن الآن
اضغط `/admin` → **📈 تحليلات الاستخدام**:
- سطر جديد: عدد الأخطاء وعدد الطلاب المتأثرين بها خلال الفترة المختارة.
- زر جديد **🐞 آخر الأخطاء**: يعرض آخر 20 خطأ خلال 7 أيام — اسم الطالب، معرّفه، هل الخطأ
  كان معالجاً محلياً (🟡) أو غير متوقع بالكامل (🔴)، نوع الاستثناء، الوقت، ونص الرسالة.
- تصدير CSV لسجل الأحداث يتضمن أخطاء الطلاب تلقائياً (نفس آلية التصدير الحالية، بما أن
  الخطأ مجرد حدث عادي بنفس الجدول)، مع تفاصيل إضافية (نوع الخطأ، traceback مختصر، هل كان
  غير متوقع) داخل عمود `metadata`.
- عند البحث عن طالب معيّن → **📈 نشاط هذا الطالب** يعرض أخطاءه ضمن آخر أحداثه تلقائياً.

## 5) الأداء وسلامة الطالب
نفس فلسفة نظام التحليلات الحالي بالضبط: التسجيل يحصل بالخلفية (`asyncio.create_task`)
دون انتظار، ولا يمكن لفشل تسجيل الخطأ نفسه أن يرمي استثناءً أو يؤخر رد الطالب. كما تم
التأكد يدوياً من عدم وجود أي حلقة استدعاء ذاتية بين `log_error()` ودالة تسجيل الأخطاء
الجديدة عند فشل الاتصال بقاعدة البيانات.
