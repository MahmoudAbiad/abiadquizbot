# باتش: تفصيل التوكنز + التكلفة التقديرية بشاشة "📊 سجل توليد الكويزات"

## قاعدة البيانات
✅ تم تطبيقها مباشرة على Supabase (مشروع `pqawoanhbgvdtbdhnbod`) — ما في شي مطلوب منك هون.
- جدول جديد `ai_model_pricing` (provider, model_name, input_price_per_million, output_price_per_million, verified, notes).
- مُعبّى بأسعار **تقريبية** لكل موديلات الكاسكيد الحالية (`gemini-3.5/3.6/3.7/3.8-flash`, Vertex, Groq) — **كلها `verified=false`**.
- `migration_ai_model_pricing.sql` مرفق فقط للتوثيق.

## ⚠️ خطوة لازم تعملها أنت: تأكيد/تصحيح الأسعار
دوّرت على الأسعار وحطيت أفضل رقم لقيته لكل موديل، بس المصادر كانت متضاربة (خصوصاً على
`gemini-3.5-flash` و`gemini-3.5-flash-lite`). **قبل ما تعتمد على رقم التكلفة**، افتح
Supabase SQL Editor وشوف/صحّح:
```sql
SELECT * FROM ai_model_pricing ORDER BY provider, model_name;
```
وعدّل أي صف غلط، مثلاً:
```sql
UPDATE ai_model_pricing
SET input_price_per_million = 0.XX, output_price_per_million = 0.XX, verified = true
WHERE provider = 'gemini' AND model_name = 'gemini-3.6-flash';
```
لغاية ما تأشّر `verified = true` عالصف، رح تظل الشاشة تحط ⚠️ جنب أي تكلفة محسوبة منه (تنبيه
بصري بس — ما بيوقف الحساب).

موديل مو موجود إطلاقاً بالجدول (مثلاً لو ضفت موديل جديد للكاسكيد لاحقاً) بيظهر "غير معروف
السعر" بدل رقم تكلفة مضلّل بصفر.

## الملفات المعدّلة (3) — بدك تستبدلها على السيرفر
1. **`helpers/supabase_helper.py`** — دالة جديدة `get_ai_model_pricing()` تجيب كل الأسعار
   من `ai_model_pricing` بكاش قصير (60 ثانية، شاشة الأدمن فقط).
2. **`handlers/files.py`** — حدث `quiz_generated` (الي بيغذّي الشاشة) صار يرفق
   `input_tokens`/`output_tokens`/`thoughts_tokens`/`total_tokens` (كانت موجودة أصلاً بنفس
   `generation_meta`، بس ما كانت مُرفقة بهالحدث تحديداً).
3. **`handlers/admin/ai_control.py`** — شاشة "📊 سجل توليد الكويزات":
   - كل صف كويز هلق فيه سطر تفصيل توكنز (`input+output[+thoughts🧠]=total`) + تكلفة تقديرية
     بالدولار (`_estimate_quiz_cost` — توكنز التفكير تُحاسَب بسعر الإخراج، نفس منطق Google).
   - هيدر الشاشة صار فيه "💰 تكلفة تقديرية إجمالية" لكل الكويزات المجلوبة (آخر 7 أيام، لا
     فقط الصفحة الحالية) — نفس منطق "متوسط زمن التوليد" الموجود أصلاً.
   - كويزات **قديمة من قبل هالباتش** (بلا بيانات توكنز بالـ metadata) بتظهر بدون سطر
     التفصيل إطلاقاً (مش أصفار مضلّلة) — طبيعي ومتوقع، رح تبلش تظهر التفاصيل من أول كويز
     يتولّد بعد الديبلوي.

## خطوات التطبيق
1. استبدل 3 الملفات المرفقة.
2. أعد تشغيل البوت.
3. **افتح `ai_model_pricing` وصحّح الأسعار** (الخطوة الأهم أعلاه) — الأرقام الحالية تقريبية فقط.
4. ولّد كويز تجريبي، وافتح لوحة الأدمن ← 🤖 التحكم بالذكاء الاصطناعي ← 📊 سجل توليد
   الكويزات — تأكد إنه التفصيل والتكلفة ظاهرين وصح.

## معروف وغير مُغطّى (نطاق مقصود)
- مسار Groq النصي السريع (`_generate_text_quiz`) ما بيرجّع توكنز إطلاقاً لسا — بالتالي
  كويزاته بتظهر بدون سطر تفصيل بهالشاشة (نفس القيد الموجود بـ `ai_generation_log` أصلاً).
- الأمر الإداري `/ai_usage` لسا ما انكتب (بند منفصل معلّق من قبل).
