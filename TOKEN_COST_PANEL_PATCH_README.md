# باتش: تفصيل التوكنز (مصدرها) + التكلفة + ترتيب الموديل بالكاسكيد — شاشة "📊 سجل توليد الكويزات"

## قاعدة البيانات
✅ تم تطبيقها مباشرة على Supabase (مشروع `pqawoanhbgvdtbdhnbod`) — ما في شي مطلوب منك هون.
- جدول جديد `ai_model_pricing` (provider, model_name, input_price_per_million, output_price_per_million, verified, notes).
- مُعبّى بأسعار **تقريبية** لكل موديلات الكاسكيد الحالية — **كلها `verified=false`** (راجع القسم أدناه).
- `migration_ai_model_pricing.sql` مرفق فقط للتوثيق.

## ⚠️ خطوة لازم تعملها أنت: تأكيد/تصحيح الأسعار
افتح Supabase SQL Editor وشوف/صحّح:
```sql
SELECT * FROM ai_model_pricing ORDER BY provider, model_name;
```
وعدّل أي صف غلط، مثلاً:
```sql
UPDATE ai_model_pricing
SET input_price_per_million = 0.XX, output_price_per_million = 0.XX, verified = true
WHERE provider = 'gemini' AND model_name = 'gemini-3.6-flash';
```
لغاية ما تأشّر `verified = true` عالصف، رح تظل الشاشة تحط ⚠️ جنب أي تكلفة محسوبة منه.

## الملفات المعدّلة (4) — بدك تستبدلها على السيرفر

### 1) `helpers/gemini_helper.py` 🆕 (جديد بهالجولة)
- `_execute_cascade()`: هلق بيحسب `cascade_rank` (ترتيب الصف، يبدأ من 1) و`cascade_total`
  (عدد صفوف الكاسكيد الكلي) *لحظة* نجاح كل توليد، ويحفظهم بـ `_last_model_used_var` مع
  الموديل/المزوّد الفائزين. **مهم:** هالرقم بينحفظ بالسجل بشكل ثابت لحظة التوليد — حتى لو
  غيّرت ترتيب الكاسكيد لاحقاً من لوحة الأدمن، السجل القديم بيضل يعرض الترتيب الفعلي وقتها.
- مسارات Super PDF/Super Images وGroq السريع (خارج الكاسكيد المرقّم أصلاً بالتصميم) بتسجّل
  `cascade_rank=None` صراحة بدل رقم وهمي — الشاشة بتعرضها بوصف مختلف (🔀/⚡) بدل رقم.

### 2) `helpers/supabase_helper.py`
- دالة `get_ai_model_pricing()` — تجيب الأسعار من `ai_model_pricing` مع كاش 60 ثانية.

### 3) `handlers/files.py`
- حدث `quiz_generated` صار يرفق: `input_tokens`, `output_tokens`, `thoughts_tokens`,
  `total_tokens`, `cascade_rank`, `cascade_total`, `generation_mode` (كلها كانت موجودة أصلاً
  بـ `generation_meta`، بس ما كانت مُرفقة بهالحدث تحديداً).

### 4) `handlers/admin/ai_control.py`
- كل صف بشاشة "📊 سجل توليد الكويزات" هلق فيه:
  - **تفصيل التوكنز بمسميات واضحة** (مو رقم مجمّع مبهم): `📥إدخال X + 📤إخراج Y [+ 🧠تفكير Z] = الإجمالي`
  - **التكلفة التقديرية بالدولار** لكل كويز (`_estimate_quiz_cost` — توكنز التفكير محسوبة بسعر الإخراج، نفس منطق Google).
  - **🆕 ترتيب الموديل بالكاسكيد لحظة التوليد**: `📶 الترتيب بالكاسكيد: #2 من 6` — أو وصف
    بديل لمسارات Super PDF/Images/Groq (خارج الترقيم بالتصميم، راجع فوق).
- هيدر الشاشة فيه "💰 تكلفة تقديرية إجمالية" لكل الكويزات المجلوبة (آخر 7 أيام).
- كويزات قديمة (من قبل أي من التحديثين) بتظهر بدون الأسطر الجديدة — طبيعي، رح تبلش تظهر
  بالكامل من أول كويز يتولّد بعد هالديبلوي.

## خطوات التطبيق
1. استبدل الملفات الأربعة (أو ديبلوي المشروع كامل من الزيب المرفق).
2. **صحّح الأسعار** بجدول `ai_model_pricing` (القسم أعلاه).
3. أعد تشغيل البوت، ولّد كويز تجريبي، وافتح 🤖 التحكم بالذكاء الاصطناعي ← 📊 سجل توليد
   الكويزات — تأكد إنه التفصيل والترتيب والتكلفة ظاهرين وصح.

## معروف وغير مُغطّى (نطاق مقصود)
- مسار Groq النصي السريع ما بيرجّع توكنز إطلاقاً لسا — بيظهر بدون سطر تفصيل توكنز/تكلفة،
  بس **مع** سطر الترتيب (⚡ مسار Groq السريع، خارج الكاسكيد).
- الأمر الإداري `/ai_usage` لسا ما انكتب (بند منفصل معلّق من قبل).
